1. supprimer le proxy server sur le front, et lancer un npm start à chaque fois sur le backend, pour enlever la clé claude du frontend, et faire pointer le chat du front sur le vrai backend.
2. il ne faut pas qu'on ai de node_module sur le front, et pas de serveur sur le front.
3. rajouter une structure asset/images et asset/csss, mettre tous les images du projet dans le dossier images et tous les feuilles de style dans css
4. créer/adapter les feuilles de style pour chaque resolution de device, utiliser des media query pour mobile.
5. il faut séparer les pages html dans des fichiers séparés, et faire attention aux URLs, séparer les pages dans des dossiers séparés.